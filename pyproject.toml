[project]
name = "media-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "filetype>=1.2.0",
    "uvicorn>=0.34.0",
]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
    "mypy>=1.14.0",
    "pytest>=8.0.0",
]

[tool.mypy]
strict = true
